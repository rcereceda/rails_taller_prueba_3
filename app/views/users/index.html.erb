<h1>Usuarios</h1>

<div class="row">
	<div class="col-md-6">
		<table>
			<% @users.each do |user| %>
				<tr>
					<td><%= user.email %></td>
					<td><%= user.role %></td>
					<td><%= link_to "admin", set_admin_user_path(user.id) %></td>
				</tr>
			<% end %>
		</table>
	</div>
</div>